# Dependencies
node_modules
.pnp
.pnp.js

# Build / Next.js
.next
out
build
dist

# Env (never commit secrets)
.env
.env.local
.env*.local
.env.bak.*
.env.development.local
.env.test.local
.env.production.local

# Emergent session handoff artifacts (apply then discard)
emergent-session-changes.tar.gz
emergent-session.patch
handoff/

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / OS
.idea
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
*.swp
*.swo

# TypeScript / testing
*.tsbuildinfo
next-env.d.ts

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Local agent tooling (session transcripts, etc.)
.claude/

# Stripe CLI (local binary; do not commit)
stripe.exe

# Vercel
.vercel
.cache

# Prisma local SQLite (do not commit dev databases)
prisma/*.db
prisma/*.db-journal

# Root-level working brand PNGs (use public/brand/ for tracked assets)
/brandIcon.PNG
/brand_Icon_and_workdmark.PNG
/brandwordmark.PNG
.gstack/

# Supabase CLI cache
supabase/.temp/
